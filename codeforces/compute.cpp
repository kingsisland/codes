#include<bits/stdc++.h>
using namespace std;
int main()
{
	cin>>p>>q>>r>>k;
	//case 1 p<q<r
	count
	for(long i=p;i<=p+k;i++)
	{
		for(long j=q;j<q+k;j++)
		{
			if((i-p)+(j-q)>k)
				break;
			// if((k-(i-p)-(j-p))>0)
			long temp=max(j,r)
			if(((k-(i-p)-(j-p))-max)>=0)
				count+=(k-(i-p)-(j-p))-temp+1;
		}
	}
}